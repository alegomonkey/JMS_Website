# Playing card images

Vendored from https://github.com/hanhaechi/playing-cards (MIT-style permissive use).
54 PNGs total: 52 standard cards named `<suit>_<rank>.png` plus `back_dark.png` and `back_light.png`.

Suits: `clubs`, `diamonds`, `hearts`, `spades`.
Ranks: `2`–`10`, plus `A`, `J`, `Q`, `K`.

Served statically from `/cards/<suit>_<rank>.png`.
